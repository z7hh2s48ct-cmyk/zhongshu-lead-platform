# 2026-09-10 清理到期 Semgrep 豁免

新仓库 Security Analysis 运行 `34456587477` 在读取到期豁免时拒绝发布。完整 Semgrep 报告表明，6 条到期记录中 `verify_production.py` 的 3 条与 `preflight_v12.py` 的 1 条已没有对应告警，因此删除陈旧记录。

另 2 条仍有告警，按实际调用边界修复：

- 迁移测试直接使用 Alembic Python API，保留升级、降级、拒绝丢弃业务数据等断言，并在结束后恢复数据库环境变量及配置缓存。
- 评审辅助脚本仅接收四个固定检查名称，使用静态参数调用当前解释器；未知名称直接拒绝，调用者不能传入任意命令数组。

没有延长豁免日期、添加忽略规则或修改安全门禁。相关迁移测试、评审辅助脚本测试和 44 项安全门禁测试通过；固定扫描镜像与固定规则对涉及文件复扫未发现告警。

上述定向结果不能替代完整主线扫描：正式发布仍须以新仓库成功的 `main` push 为来源，验证全源码扫描覆盖、Trivy、SBOM 和最终镜像摘要一致。
